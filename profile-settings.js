const PROFILE_URL='https://tasoycsbzceohgjxizdp.supabase.co',PROFILE_KEY='sb_publishable_ZLU-d0ZI-XT_IgabzmigkA_5vohKjLl';
const ps=id=>document.getElementById(id);let profile=null;
function psSession(){try{return JSON.parse(localStorage.getItem('credence_session')||'null')}catch{return null}}
function psEsc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
async function psApi(path,opt={}){const s=psSession();if(!s?.access_token)throw Error('Please log in first.');const r=await fetch(`${PROFILE_URL}/rest/v1/${path}`,{...opt,headers:{apikey:PROFILE_KEY,Authorization:`Bearer ${s.access_token}`,'Content-Type':'application/json',...(opt.headers||{})}});if(!r.ok){let d={};try{d=await r.json()}catch{}throw Error(d.message||'Profile request failed')}return r.status===204?null:r.json()}
async function loadProfileSettings(){const s=psSession();if(!s?.user?.id||!ps('profilePanel'))return;try{const p=await psApi(`profiles?id=eq.${encodeURIComponent(s.user.id)}&select=id,username,full_name,email,phone,avatar_url,accent_color,preferences,created_at&limit=1`);profile=p[0]||{};ps('profileName').value=profile.full_name||'';ps('profilePhone').value=profile.phone||'';ps('profileUsername').value=profile.username||'';ps('profileAvatar').value=profile.avatar_url||'';const pref=profile.preferences&&typeof profile.preferences==='object'?profile.preferences:{};ps('prefEmail').checked=pref.email_notifications!==false;ps('prefAnnouncements').checked=pref.announcements!==false;ps('profileEmail').textContent=profile.email||s.user.email||'—';ps('profileJoined').textContent=profile.created_at?new Date(profile.created_at).toLocaleDateString('en-IN'): '—';ps('profilePanel').hidden=false}catch(e){if(ps('profileNotice')){ps('profileNotice').textContent=e.message;ps('profileNotice').className='notice'}}}
async function saveProfileSettings(e){e.preventDefault();const s=psSession();if(!s?.user?.id)return;const notice=ps('profileNotice');notice.textContent='Saving…';notice.className='notice';try{const preferences={email_notifications:ps('prefEmail').checked,announcements:ps('prefAnnouncements').checked};await psApi(`profiles?id=eq.${encodeURIComponent(s.user.id)}`,{method:'PATCH',headers:{Prefer:'return=minimal'},body:JSON.stringify({full_name:ps('profileName').value.trim(),phone:ps('profilePhone').value.trim()||null,username:ps('profileUsername').value.trim()||null,avatar_url:ps('profileAvatar').value.trim()||null,preferences})});notice.textContent='Profile saved successfully.';notice.className='notice success';if(typeof refreshStudent==='function')await refreshStudent()}catch(e){notice.textContent=e.message;notice.className='notice'}}
function openProfileSettings(){loadProfileSettings()}
document.addEventListener('DOMContentLoaded',()=>{ps('profileForm')?.addEventListener('submit',saveProfileSettings);ps('profileBtn')?.addEventListener('click',openProfileSettings);ps('closeProfile')?.addEventListener('click',()=>{ps('profilePanel').hidden=true});ps('profilePanel')?.addEventListener('click',e=>{if(e.target.id==='profilePanel')e.currentTarget.hidden=true});if(psSession())loadProfileSettings()});
window.CREDENCE_PROFILE={load:loadProfileSettings,open:openProfileSettings};