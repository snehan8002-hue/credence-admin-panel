const REG_URL='https://tasoycsbzceohgjxizdp.supabase.co',REG_KEY='sb_publishable_ZLU-d0ZI-XT_IgabzmigkA_5vohKjLl';const regEl=id=>document.getElementById(id);function regSession(){try{return JSON.parse(localStorage.getItem('credence_session')||'null')}catch{return null}}function regNotice(text,ok=false){const n=regEl('regNotice');n.hidden=false;n.textContent=text;n.className='notice '+(ok?'success':'fail')}function closeRegistration(){regEl('regModal').hidden=true;regEl('registrationForm').reset();regEl('regNotice').hidden=true}async function createRegistration(e){e.preventDefault();const session=regSession();if(!session?.access_token)return regNotice('Admin login required.');const password=regEl('regPassword').value;if(password.length<8)return regNotice('Password must be at least 8 characters.');const b=regEl('createRegistration');b.disabled=true;b.textContent='Creating…';try{const body={action:'create_user',role:regEl('regRole').value,email:regEl('regEmail').value.trim().toLowerCase(),password,full_name:regEl('regName').value.trim()};const phone=regEl('regPhone').value.trim();if(phone)body.phone=phone;const r=await fetch(REG_URL+'/functions/v1/credence-admin',{method:'POST',headers:{apikey:REG_KEY,Authorization:'Bearer '+session.access_token,'Content-Type':'application/json'},body:JSON.stringify(body)});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Registration failed');regNotice('Registration created successfully. No OTP is required.',true);setTimeout(()=>location.reload(),900)}catch(err){regNotice(err.message||'Registration failed.')}finally{b.disabled=false;b.textContent='Create registration'}}window.addEventListener('DOMContentLoaded',()=>{regEl('newRegistration').addEventListener('click',()=>{regEl('regModal').hidden=false;regEl('regName').focus()});regEl('closeRegistration').addEventListener('click',closeRegistration);regEl('cancelRegistration').addEventListener('click',closeRegistration);regEl('registrationForm').addEventListener('submit',createRegistration)});